(function($) {
  BNUOJ.Events = {
    // AJAX form submission complete successfully
    // @param responseJSON    JSON returned by server
    AJAX_FORM_CORRECT: "ajax_form_correct"
  }
}).call(this, jQuery);
