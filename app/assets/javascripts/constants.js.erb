// Initialize BNUOJ object.
window.BNUOJ = {
  // Common Utilities.
  Utils: {},
  // View controllers.
  Views: {}
};

// Inject global configs.
window.globalConfig = (<%= JS_CONFIG.to_json %>);

// Set locale
I18n.defaultLocale = "<%= I18n.default_locale %>";
I18n.locale = "<%= I18n.locale %>";
