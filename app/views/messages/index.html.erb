<h1><%= t "message.titles.list" %></h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @messages.each do |message| %>
      <tr>
        <td><%= link_to t("global.links.show"), message %></td>
        <td><%= link_to t("global.links.edit"), edit_message_path(message) %></td>
        <td><%= link_to t("global.links.delete"), message, method: :delete, data: { confirm: t("global.prompts.is_sure") } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Message', new_message_path %>
